# t_string = (3A)  #3a is 58


def hex_to_binary(hex_number):
   
    hex_string = "3A"

#im then gonna convert the hex to int and then to binary
    
    binary_num = bin    
    binary_num = bin(int(hex_string))#return the hex num as an integer in binary
    
    
    return (bin)#return menas to emd the function
print((bin), int) 
